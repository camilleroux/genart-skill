node_modules

# Created by the local `npm i -D playwright` documented in AGENTS.md. The plugin
# ships zero dependencies and no package.json — never commit these. Anchored, so
# a fixture that legitimately needs its own package.json is not silently ignored.
/package.json
/package-lock.json
